# Этап 12. Финальные тесты, приемка и фиксация итогового состояния главы 5

## Назначение этапа

Этап 12 завершает программный блок главы 5. На этапе выполняется финальная приемка артефактов: проверяется наличие выходных файлов, согласованность числа строк, диапазоны прогнозных показателей, интервальные ограничения и признаки методической безопасности априорного расчета.

## Реализованные изменения

- Добавлен модуль `chapter5_acceptance.py`.
- Добавлен CLI-флаг `--run-acceptance`.
- Добавлены выходные артефакты:
  - `reports/chapter5/chapter5_acceptance_report.json`;
  - `reports/chapter5/chapter5_acceptance_report.md`.
- Добавлены тесты финальной приемки.

## Контрольный запуск

Команда:

```powershell
python -m manual_coding_sim.prediction.chapter5_runner `
  --project-root . `
  --config configs/chapter5.yaml `
  --run-full-pipeline `
  --build-report `
  --run-acceptance
```

Ключевой результат:

```text
Финальная приемка главы 5: выполнена.
Статус приемки: пройдена.
Проверок выполнено: 18
```

## Результаты приемки

- Статус приемки: `пройдена`.
- Число проверок: `18`.
- Все проверки имеют статус `true`.
- Все ключевые таблицы содержат `150` строк.
- Диапазон `Q_pred`: `[0.10344477176221134; 0.8893015161715213]`.
- Диапазон `uncertainty_score`: `[0.0994170991788137; 0.5386938309397353]`.
- Диапазон `interval_radius`: `[0.014912564876822; 0.0808040746409603]`.
- Методическая безопасность подтверждена:
  - `apriori_only = true`;
  - `leakage_check_passed = true`;
  - `forbidden_column_count = 0`;
  - `full_pipeline_completed = true`.

## Контроль тестов

- `tests/prediction`: `69 passed`.
- Полный `pytest`: `317 passed`.

## Итог

Этап 12 реализован и готов к пользовательской проверке. После локального подтверждения тестов, неизменности `prior_features.csv` и фиксации хешей программный блок главы 5 можно считать завершенным.
